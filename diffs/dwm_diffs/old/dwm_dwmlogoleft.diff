diff --git a/config.mk b/config.mk
index b6eb7e0..7084c33 100644
--- a/config.mk
+++ b/config.mk
@@ -1,5 +1,5 @@
 # dwm version
-VERSION = 6.3
+VERSION = 6.2
 
 # Customize below to fit your system
 
diff --git a/dwm.c b/dwm.c
index a96f33c..6120d10 100644
--- a/dwm.c
+++ b/dwm.c
@@ -431,7 +431,7 @@ buttonpress(XEvent *e)
 		focus(NULL);
 	}
 	if (ev->window == selmon->barwin) {
-		i = x = 0;
+		i = x = -2; /* default: 0, but I need to disable clickable tags */
 		do
 			x += TEXTW(tags[i]);
 		while (ev->x >= x && ++i < LENGTH(tags));
@@ -718,6 +718,12 @@ drawbar(Monitor *m)
 			urg |= c->tags;
 	}
 	x = 0;
+
+    /* DWM logo left before tags */
+    char *lstext = "    "; /* <- I used JetBrainsMono Nerd Font Bandit */
+	w = blw = TEXTW(lstext);
+	x = drw_text(drw, x, 0, w, bh, lrpad / 2, lstext, 0);
+
 	for (i = 0; i < LENGTH(tags); i++) {
 		w = TEXTW(tags[i]);
 		drw_setscheme(drw, scheme[m->tagset[m->seltags] & 1 << i ? SchemeSel : SchemeNorm]);
